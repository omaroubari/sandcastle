---
"@ai-hero/sandcastle": patch
---

Remove deprecated `worktree` field from `RunOptions` and delete `WorktreeMode` type. Branch strategy is now configured exclusively on the sandbox provider via `branchStrategy`.
